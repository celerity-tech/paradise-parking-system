mutation UpdateParkingSession($input: UpdateParkingSessionInput!) {
    updateParkingSession(input: $input) {
        id
        vehicleType
        plateNumber
        enteredAt
        exitedAt
        durationMinutes
        parkingFee
        paymentStatus
        parkingState
        rateType
        includeInBIRReport
    }
}
